include /include/blocks/catalog/setItem/setItem

mixin setList(title)
	+b.setList&attributes(attributes)
		+e.box.box
			+e.title #{title}
			+e.items
				+e.content
					+setItem.__item
					+setItem.__item
					+setItem.__item
					+setItem.__item
					+setItem.__item
					+setItem.__item
					+setItem.__item
